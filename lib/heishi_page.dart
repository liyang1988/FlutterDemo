import 'package:flutter/material.dart';

class HeiShiPageControler extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: Text("黑拾测试"),
      ),
      backgroundColor: Colors.blue,
    );
  }
}
