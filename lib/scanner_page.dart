import 'package:flutter/material.dart';

class ScannerPageControler extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      // appBar: topBar,
      appBar: AppBar(
        title: Text("扫码"),
      ),
      backgroundColor: Colors.green,
    );
  }
}
